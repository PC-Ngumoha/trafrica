/**
 * helpers.js contains the helper functions for our code
 */

// Combines class name strings for utility in styling components
export const concatClasses = (...classes) => {
  return [...classes].join(' ');
};

